//= 원본 코드
function originalCode(w, h, mapArr, x1, y1, x2, y2) {
	// 배열을 초기화
	var resArr  = [];

	// 처리
	resArr.push(x1, y1);
	resArr.push(x2, y2);

	// 처리 결과를 담은 배열을 돌려주고 종료
	return resArr;
}

